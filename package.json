{
  "name": "twitch-auth-proxy",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.17",
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.4",
    "vitest": "~1.3.0",
    "wrangler": "^3.0.0"
  }
}
